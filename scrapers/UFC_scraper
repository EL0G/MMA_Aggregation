import re
import requests
from bs4 import BeautifulSoup

content = requests.get("https://www.cbssports.com/ufc/schedule/results/")
content.raise_for_status()

soup = BeautifulSoup(content.text, "lxml")
events_list = soup.find_all(class_="UfcSchedule-info")

for event in events_list:
    parts = list(event.stripped_strings)
    date = parts[0]
    title = parts[1]


from bs4 import BeautifulSoup
import requests, http, json

content = requests.get("https://pflmma.com/events")
soup = BeautifulSoup(content.text, 'lxml')

events = soup.find_all(class_= 'event-card-info p-4')
for event in events:
    title = event.find('h3')
    date = event.find('h6')
    
#NORMALIZE DATE FORMAT
